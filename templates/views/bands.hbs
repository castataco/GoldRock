<div class="container">
	<div class="row">
		<div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
			<article>
				<p><a href="/bands">&larr; back to the bands</a></p>
				<hr>
				<header>
					<h1>{{data.bands.title}}</h1>
					<h5>
						{{#if author.name.first}}by {{author.name.first}}{{/if}}
					</h5>
				</header>
				<div class="bands">
					{{{data.bands.content.extended}}}
				</div>
			</article>
		</div>
	</div>
</div>
